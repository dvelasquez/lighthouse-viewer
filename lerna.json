{
  "command": {
    "publish": {
      "ignoreChanges": ["ignored-file", "*.md", "**/packages/**/demo/**"],
      "message": "chore(release): publish"
    }
  },
  "packages": [
    "packages/**"
  ],
  "version": "independent",
  "useWorkspaces": true
}
